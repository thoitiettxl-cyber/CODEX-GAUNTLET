// Provider quota APIs are opt-in. Add an adapter here only after its credential
// use, endpoint, response bounds, and normalization contract are reviewed.
export const defaultQuotaAdapters = new Map();
