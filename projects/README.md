# In-repository projects

Place each project managed inside this repository in its own direct child
directory under `projects/`. The directory name is the project's stable scope
identifier, and project-specific instructions remain inside that directory.

Do not mirror external projects here. Declare their location and entrypoint in
`scopes/external.json` instead.
