# Claude Code entry map

`AGENTS.md` is the authoritative repository instruction map. Read and follow it
before acting; use its context routing instead of duplicating runtime-specific
instructions here.
